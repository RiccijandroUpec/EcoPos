//    uniCenta oPOS - Touch Friendly Point Of Sale
//    Copyright (c) 2009-2014 uniCenta
//    http://sourceforge.net/projects/unicentaopos
//
//    This file is part of uniCenta oPOS.
//
//    uniCenta oPOS is free software: you can redistribute it and/or modify
//    it under the terms of the GNU General Public License as published by
//    the Free Software Foundation, either version 3 of the License, or
//    (at your option) any later version.
//
//    uniCenta oPOS is distributed in the hope that it will be useful,
//    but WITHOUT ANY WARRANTY; without even the implied warranty of
//    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
//    GNU General Public License for more details.
//
//    You should have received a copy of the GNU General Public License
//    along with uniCenta oPOS.  If not, see <http://www.gnu.org/licenses/>.
// **************************************************************************
// Ticket.Close
// This script displays the amount of Cash Change (CashTendered - TicketValue) to be given to Customer

import java.awt.Font; 
import javax.swing.plaf.FontUIResource; 
import com.openbravo.pos.payment.PaymentInfo;
import javax.swing.JOptionPane;
import javax.swing.JDialog;

boolean isCash = false;
String change = "";
PaymentInfo p = ticket.payments.getFirst();

if ("cash".equals(p.getName())) {
    isCash = true;
    change = p.printChange();
}

// Get details of the original font before we change it otherwise all dialogboxes will use new settings
JOptionPane pane = new JOptionPane();
Font originalFont=pane.getFont();

UIManager.put("OptionPane.buttonFont", new FontUIResource(new Font("ARIAL",Font.PLAIN,20)));
message = "Change: " + change;
 JLabel FontText = new JLabel(message);
 FontText.setFont (new Font ( "Arial", Font.BOLD, 36) );

if(isCash) {
     JOptionPane pane = new JOptionPane();
     pane.setMessage(FontText);
     pane.setPreferredSize( new Dimension(450,150));
     JDialog dialog = pane.createDialog("Cash payment");
    new Thread(new Runnable() {
         void run() {
            dialog.show();
        } 
    }).start();
}

// Return to default settings
UIManager.put("OptionPane.buttonFont", new FontUIResource(new Font(originalFont.getName(),originalFont.getStyle(),originalFont.getSize())));

// --- ecopos-sri-connector hook -------------------------------------------
// Signal that a normal sale just closed by dropping a flag file. This is a
// DATA-ONLY change to this script resource - no ECOPos .java file is
// modified. ecopos-sri-connector (a separate process/JVM) watches the
// "sri-conector/pendientes" folder and, on seeing a new flag file, reads
// the full ticket from the database itself and takes it from there. If
// this folder doesn't exist or the write fails for any reason, the sale
// must never be affected - so every step here is wrapped and swallows
// its own errors.
//
// Whether to invoice THIS ticket is controlled by the cashier via the
// "Facturar SRI: SI/NO" buttons in the ticket screen (script.SriInvoiceOn.txt
// / script.SriInvoiceOff.txt), which set/clear the "sri.invoice" ticket
// property. Default is "false" (no invoice) if the cashier never touched
// the buttons - the ticket always prints normally regardless of this flag,
// since printing happens through a completely separate flow.
try {
    boolean facturarSri = "true".equals(ticket.getProperty("sri.invoice", "false"));
    if (facturarSri && ticket.getTicketType() == com.openbravo.pos.ticket.TicketInfo.RECEIPT_NORMAL) {
        java.io.File dirname = new java.io.File(System.getProperty("dirname.path", "./"));
        java.io.File pendientes = new java.io.File(dirname, "sri-conector/pendientes");
        pendientes.mkdirs();
        java.io.File flag = new java.io.File(pendientes, ticket.getId() + ".flag");
        java.io.FileWriter fw = new java.io.FileWriter(flag);
        fw.write(String.valueOf(System.currentTimeMillis()));
        fw.close();
    }
} catch (Exception eSri) {
    // Never let a problem here interrupt the till - just leave no flag
    // file, ecopos-sri-connector's own reconciliation pass can catch up
    // later by comparing TICKETS against ecopos_sri_comprobantes.
}